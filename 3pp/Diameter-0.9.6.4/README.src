This package contains source code for the dk.i1.diameter.* packages.
You can see the version in the accompanying 'version' file.

The build process (Makefile) requires the JavaSCTP package, but you can build
without it if you comment out the right lines in the makefile.

Please read the LICENSE file before using.
