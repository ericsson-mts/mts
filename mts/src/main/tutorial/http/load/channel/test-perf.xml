<?xml version="1.0" encoding="UTF-8"?>
<!-- Related XMLSchema file: conf/schemas/test.xsd -->

<test name="Load tests">

        <!--
           - local host and port for HTTP client
         -->
        <parameter name="[Local-Host]" operation="system.ipaddress"/>
        <parameter name="[Local-Host]" operation="set" value="[Local-Host(0)]"/>
        <parameter name="[Local-Port]" operation="set" value="10000"/>

        <!--
           - URL of the HTTP server.
         -->
        <parameter name="[Remote-Host]" operation="system.ipaddress"/>
        <parameter name="[Remote-Host]" operation="set" value="[Remote-Host(0)]"/>
        <parameter name="[Remote-Port]" operation="set" value="8080"/>
        <parameter name="[Remote-URL]" operation="set" value="http://[Remote-Host]:[Remote-Port]" />

	    <!--                   -->
    	<!-- RUNPROFILE PARAMETERS -->
    	<!--                   -->
    	<runProfile>
            <step time="0" frequency="1000"/>
            <end delay="300"/>
    	</runProfile>

  		<testcase name="client HTTP" description="load [number]" state="true" number="1">    			
			 <scenario name="client HTTP">client.xml</scenario>
		</testcase>
	
</test>
